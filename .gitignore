# Upstream repositories cloned for reference only — never vendored into this project.
# Contains GPL-3.0 code (meshtastic-firmware); keep it out of the tree.
/reference/

# Third-party tooling, reproducible from upstream releases:
#   tools.zip from badgeteam/esp32-component-badgelink releases
#   + libusb-1.0.dll (VS2022 MS64) from libusb/libusb releases
#   + a local venv
/tools/badgelink/

# Internal working material: design notes, implementation plans, investigation
# write-ups and scratch analysis. Published documentation lives in /docs and is
# tracked; anything below is a working draft and stays local.
/notes/
/plans/
/design/
/scratch/
/private/
/wip/
TODO.md
NOTES.md
PLAN.md
*.plan.md
*.design.md
*.draft.md
*.notes.md
*.internal.md
*-plan.md
*-design.md
*-draft.md
*-notes.md
*-internal.md

# Captured device state and session recordings — these come off a real radio and
# can contain node names, keys, coordinates and message contents.
*.log
session*.log
nodes-*.bin
*.nvs
*.capture
/captures/

# Machine-local ESP-IDF location markers (read by the upstream Makefiles)
.IDF_PATH
.IDF_TOOLS_PATH

# ESP-IDF build output
build/
sdkconfig
sdkconfig.old
# Generated per-device from sdkconfigs/general + sdkconfigs/<device>; those two
# ARE tracked, the merged result is not.
sdkconfig_*
dependencies.lock
managed_components/
*.bin
*.elf
*.map

# ESP-IDF / toolchain local environment
.env
env
.vscode/
!.vscode.template/
compile_commands.json
.cache/
.clangd/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors
*.swp
*~
.idea/

# OS
Thumbs.db
desktop.ini
.DS_Store
